# 待办：同一轮内多个工具并发执行

## 现状

模型一轮返回多个 `tool_call` 时，sai 逐个执行：

```rust
// src/agent/mod.rs:278
for (provider_call, execution_call) in prepared_calls.calls {
```

读三个文件就是三轮往返。

已经具备的并发能力不在这一层：

- `src/tools/subagent.rs:280` 用 `tokio::spawn` 后台跑子智能体，完成时发 system-reminder。粒度重（独立会话、可选 worktree 隔离），适合委派任务，不适合"读五个文件"
- `src/agent/mod.rs:511` 请求权限的同时后台跑审核模型，超时静默回退人工

## 目标

同一轮内的多个只读工具调用并发执行，其余保持串行。

## 难点

1. **权限交互要插队**。`prepared_calls.question_policy` 已经处理了 `ask_question` 的一部分（`defer_sibling_tools`），并发后要重新审视：一个调用弹出权限请求时，同批其它调用是继续跑还是一起挂起
2. **事件顺序**。`tool_event_seq` 当前按循环递增，前端轨迹和工具卡依赖它排序。并发后 seq 要在派发前分配，不能按完成顺序
3. **失败隔离**。一个调用失败不应中断同批其它调用，但要保证 tool_call_id 与结果一一对应地回填
4. **写操作不并发**。同时写同一个文件会互相覆盖。判据可以复用 `ToolSpec::writes()` 标记：全部只读才并发，含写操作则退回串行

## 建议实现顺序

1. 先给 `prepared_calls` 加分组：连续的只读调用归一组，写操作各自成组
2. 组内 `join_all`，组间保持顺序
3. seq 在分组时预分配
4. 权限请求出现时，该组降级为串行

## 为什么值得做

收益覆盖所有工具，不只编辑。这也是"一次多处编辑"的正解——比新增 `multi_edit` 工具更通用，后者只解决编辑一种情况。

## 相关

同批还有一个更便宜、收益也明确的改动：编辑工具的 Read-before-Edit 硬约束（见下）。

---

# 待办：编辑工具的先读约束

## 现状

`src/tools/str_replace.rs` 只检查文件存在，不要求模型读过它。

## 目标

`str_replace` / `write_file` 在本会话未读过目标文件时拒绝执行。

## 价值

消除一整类失败：模型凭记忆拼 `old_string` 去改文件，命中错误位置。有了约束，`old_string` 一定来自真实读过的内容。

连带收益：正因为"编辑失败必然报错"，工具描述里才能写「不要为了验证而重新读取刚编辑过的文件」，省掉大量无用读取。

## 实现

会话级的已读文件集合，`read_file` 时记入，编辑工具执行前检查。进一步可记 mtime，检测读取之后被外部修改的情况。
