import { SourceControlPane } from "../source-control/source-control-pane";

/**
 * 保留工作区 Diff 标签入口并渲染 Source Control 面板。
 *
 * @returns Source Control 面板
 */
export function DiffPane() {
  return <SourceControlPane />;
}
